
FILE(GLOB_RECURSE HRI_GUI_SRC "*.cpp")

ADD_LIBRARY(hri-gui STATIC ${HRI_GUI_SRC})
TARGET_LINK_LIBRARIES(hri-gui ${LIBS})

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/hri-gui)
SET(COMPONENTS ${COMPONENTS} hri-gui PARENT_SCOPE)
		
